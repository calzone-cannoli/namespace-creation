---
name: Create namespaces
hosts: localhost
gather_facts: false
tasks:
  - name: Create namespaces
    kubernetes.core.k8s:
      name: testing
      api_version: v1
      kind: Namespace
      state: present