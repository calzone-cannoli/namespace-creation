---
- name: Create namespaces
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Create namespaces
      kubernetes.core.k8s:
        name: "{{ app_name }}"
        api_version: v1
        kind: Namespace
        state: present