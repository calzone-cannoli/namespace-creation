- name: Set vars
  ansible.builtin.set_fact:
    ocp_instance: "{{ ocp_instances[business_unit | lower][item.environment | lower | regex_replace('\\d', '')] }}"
    env_label: "{{ business_unit | lower }}_{{ creds[business_unit | lower][item.environment | lower | regex_replace('\\d', '')] }}"
# - name: Print namespaces
#   ansible.builtin.debug:
#     msg: 
#     - "env {{ item.environment }} has size {{ item.size }}"
#     - "OCP URL: {{ ocp_instance }}"
#     - "{{ vars[env_label] }}"