---
- name: Hello world
  hosts: localhost
  gather_facts: false
  tasks:
    # - name: Greet the world
    #   ansible.builtin.debug:
    #     msg: "hello world!!"
    - name: Check ritm
      ansible.builtin.debug:
        var: ritm
    - name: Check vars
      ansible.builtin.debug:
        msg: "{{ item.namespace }}"
      loop: "{{ namespaces }}"
