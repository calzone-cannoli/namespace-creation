---
- name: Hello world
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Greet the world
      ansible.builtin.debug:
        msg: "hello world!!"
    - name: Check vars
      ansible.builtin.debug:
        msg:
          - "{{ diagnostic_dev }}"
          - "{{ diagnostic_stg }}"
